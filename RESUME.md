# 引継ぎ（2026-09-04 の時点）

家の Claude へ。この節を最初に読めば続きから入れる。以下は全部 push 済み。
https://github.com/yomei-o/soko_ban_wasm — Pages で動く:
**https://yomei-o.github.io/soko_ban_wasm/**

`index.html` はバージョンを付けられないので、ブラウザ確認は **Ctrl+Shift+R**。
音は最初の操作か「音を出す」ボタンで始まる（ブラウザが操作なしの再生を
許さないため）。

## いまの状態

**30 面すべて遊べて、絵も音も鳴っている。2026-09-04 に user が
「ほぼ完璧」と確認。**

user 確認済み:

* 絵は全部合っている
* 起動ロゴは花王 → THINKING RABBIT の順（同時ではない）
* 歩数の表示は SCORE 窓の中
* 音の速さは正しい
* **曲の種類は合っている**（プレイ中 = BGM 2、クリア = BGM 4 に直したあと）
* チャンネルのずれ・主旋律・クリアの絵の出方（2026-09-03 に直したぶん）
* **色（プレーンとビットの対応）と画面切り替えのフェードアウト**
  （2026-09-04、下の 2 節）

**エンディングも入った**（2026-09-04、下の節）。残りは TRACE / OPTION /
EDIT と記録の保存、モノクロモード。

## 2026-09-04 その 1 — 色が違っていた（プレーンとビットの対応）

user から「荷物が所定の場所に行くと花王のロゴになる。花王のロゴは緑の
マークなのに色が違う。パレット系を調べて」と言われた件。

**パレットは合っていた。** 3 本の表（DS:02a0 / 02d0 / 0300）は
`disk/SBP98.EXE` とバイト単位で一致するし、成分が R G B なのも
`setpal` の出力先（先頭バイト → ポート 0xAC = 赤）で確かめた。

違っていたのは**プレーンをパレット番号のどのビットに置くか**。絵は
a800 / b000 / b800 / e000 の順に読み込むので「プレーン p がビット p」と
読みたくなるし、PC-98 の教科書（a800 青・b000 赤・b800 緑・e000 輝度、
番号 = 輝度緑赤青）もそう。**原作はそう描いていない。**

色 c で塗るとき、ライブラリは GRCG のタイルレジスタ 4 本を、プレーン順に
`00` / `ff` の 16×4 の表から流し込む。表は `CS:0x1c2b`、ファイル `0xb18b`:

```
色 1   ff 00 00 00      ビット 0 -> プレーン 0   a800
色 2   00 00 ff 00      ビット 1 -> プレーン 2   b800
色 4   00 ff 00 00      ビット 2 -> プレーン 1   b000
色 8   00 00 00 ff      ビット 3 -> プレーン 3   e000
```

`src/cg.c` の `CG_PLANE_BIT = { 0, 2, 1, 3 }` がそれ。直すと 3 つ揃う:

* `FUN_1edb_109b` が盤面の地を塗る色 3（`#ffddbb`）と**床タイルの色が一致**
  する。誤読だと床だけ色 5（`#ffbb33`）で、クリーム色の紙にオレンジの升目
* NIVEA の缶（スプライト 0x24）が青（`#0022aa`）になる。誤読では黄緑
* ゴールに乗った荷物の三日月と「花王」が**緑**（`#009966`）。誤読ではオリーブ
* おまけに、タイトルとエンディングで「倉庫番」が同じ赤になる（誤読では
  黄色と緑でばらばらだった）

**教訓**: 「教科書どおり」で埋めたところは、原作の中に同じことを言っている
表が無いか探す。ここでは GRCG の表がそれだった。絵の見た目で悩むより速い。

検査は `tmp/game_check.exe` に 4 本（誤読に戻すと 4 本とも落ちる）。

## 2026-09-04 その 2 — 画面が変わるときのフェードアウト

原作は曲を止める手段をこれしか持っていない。`FUN_24d7_001d(n)` は 6 面とも
`FUN_24d7_00a8` を「いま = 0」で呼び、その道は

```
AH=0x06 速さ [0x04a6] = 8 でフェード開始
while (AH=0x08) ;            ドライバが止まったと言うまで空回り
AH=0x10 / AH=0x01            次の曲
```

**待っているあいだゲームは止まる。** 空回りの中では絵も描かないしキーも
読まないので、前の画面が出たまま約 2.9 秒過ぎてから次の画面になる。
面に入るとき（1edb:1053）もクリアのとき（1edb:11c7）も、bgm は**描く前**に
呼ばれる。

ドライバ側は `[0x0f6e]` に `速さ|0x80` を置き、音量を書く `0x08a8` が
呼ばれるたびに (数え, 減衰) を進める。FM は +4 で 0x7f 止め、SSG は +1 で
15 止め。FM 3 本の減衰が `[0x0f7d] = 0x40` を全部越えたら `0x048d` が
player を畳んで `[0x0cc8]` を回しはじめ、バイトが 0 に戻ると `[0x0f80] = 0`。
速さ 8 で **616 割り込み = 2.84 秒**（`sound_check` が測る）。

移植は `mmd2_fade()` / `fade_level()` / `mmd2_tick()` の頭、ゲーム側は
`app_music()` と `app_tick` の待ち。待ちのあいだ `app_busy()` が 1 を返し、
盤面や面選択への切り替えは待ちが明けてから起きる（`app_play` → `play_now`、
`go_select` → `select_now`）。

**この移植だけの事情**: ドライバの時計は音声コールバックなので、ブラウザが
まだ音を鳴らしていないと待ちが明けない。`BGM_WAIT_TICKS = 200` フレームで
諦める安全弁を置いた。音が鳴っていれば必ず先にドライバが終わる。

詳しくは [docs/sound.md](docs/sound.md) の「フェードアウト」。

## この日の最後に直したこと — チャンネルのずれ

`src/mmd2.c` の `step_code` で **ループ終了（コード 114）が 1 ティック
消費していた**。

原作の `0x076a` は素の `ret` で戻り、`0x063b` の `jmp 0x618` が**同じ
パスの中で次のコードを読む**。フェッチループから抜けるのは音（`0x063d`）と
休符（`0x068e`）だけで、それも戻り番地を `pop` するという形でやっている。

つまりループ終了はティックを食わない。食わせていたので、**1 周ごとに 1
ティック遅れ**、周回の速いトラックほど余分に遅れて、時間が経つほど
チャンネル間が開いていった。コード 252 のジャンプも同じ間違いだった。

検査を足した（`tmp/sound_check.exe`）:

```
トラック 0: loop { 96 ティックの音 }        1 周 = 1 音
トラック 1: loop { 48 ティックの音 }        1 周 = 1 音、周回は倍
```

周回数が倍違うので、ループ終了がティックを食うと片方だけ余分に遅れる。
1 分後と 4 分後に「トラック 1 の音数 == トラック 0 の 2 倍」を測り、
**ずれが増えないこと**を見る。

* 直った状態: 1 分 34/68（差 0）、4 分 136/272（差 0）
* バグを戻すと: 1 分 34/67（差 −1）、4 分 135/266（差 −4）と**増える**

2 音を 1 周に入れる形では両トラックが同じ回数ループしてしまい、同じだけ
遅れて**ずれが見えない**。それで気付けなかった。

## 主旋律が聞こえなかった原因 — スライドと音量の効き

**1. 音量の効きが原作と違っていた。** `0x08a8` の FM 側（`0x093b`）は

* alg 0〜3（キャリア 1 本）は**レベルをそのままレジスタ 0x4c+ch へ**（`0x0959`）
* alg 4〜7 は**キャリアの TL の最小値**を音色から取り、
  `音色のTL + (レベル − 最小値)` を各キャリアへ（`0x0965` / `0x0978` /
  `0x0994` → `0x09d5`）。キャリア間の差は保たれ、一番大きいキャリアが
  ちょうどレベルに来る

私は全キャリアに同じ値を書いていたので、3 キャリアの声（旋律の音色 6 は
alg 6）が音量の指定よりずっと大きくなり、飽和して潰れていた。

**2. スライドを止める場所が違っていた。** 原作は `0x0603`（**フェッチの
直前**）で `[si+0x10] = 0` としてスライドを止め、そのあとコードを読む。
曲は `slide` → 音 の順に書くので、音のハンドラで止めると**直前に置かれた
スライドを打ち消してしまう**。私はそうしていたので、旋律が滑らずに固い点に
なっていた。

**3. 範囲 9 / 10 が未実装だった。** どちらも 3 バイトの雛型を
`[si+0x0b]`〜`[si+0x13]` に写して毎ティック歩かせるもの:

* 範囲 9（コード 115..146 の前半、表は `.VOI+0x00`）= **音量の包絡**。
  `period` ごとに `[si+8]`（レベル）を `delta` ずつ動かし `limit` で止まる
  （`0x05a5`。8 ビット加算の桁上がりで limit がどちら向きの止めかが決まる）
* 範囲 10（表は `.VOI+0x30`）= **音程スライド**。`period` ごとに
  `[si+4]`（音程の語）に符号付き 16 ビットの `delta` を足す。上限なし
  （`0x05e6`）

直した結果、旋律トラックは 778 → 745 → 703 → 664 → 618 → … → 150 Hz と
滑らかに下がる下降ポルタメントになった（20ms 窓で実測）。

## クリアの絵はディゾルブだった

`FUN_23b0_0440` は絵を貼るのではなく**21 段で散らしながら入れる**:

```
ax = 0
外: bx = ax
  内: out 0xa6,1 で絵のページを読み、4 プレーンを OR して cx
      out 0xa6,0 で表示ページへ戻り、not cx で「絵が無い所」
      各プレーン: [bx] &= cx ; [bx] |= その語
      bx += 0x2a                42 バイト進む
  while bx < 0x8000
  ax += 2
while ax != 0x2a                21 回、開始は 0,2,4 … 40
```

42 バイトは 1 行の半分強なので、1 段ごとに斜めに散った疎な模様が乗り、
21 段で埋まる。「絵が無い所」は 4 プレーンの OR なので **色 0 が透明**。

雲は**色 1 と色 0 の市松（50% 網）**で、タイルパレットでは色 0 も 1 も
`000` なので、絵単体で見ると真っ黒に見えるが、盤の上では半分透けて灰色の
網に見える。**これが原作の見え方。**

`CLEARM.CG` と `PEKEM.CG` はマスクではなく、8 色パス（`FUN_23b0_04bc`）用の
**モノクロ版の絵**。

## おじさんの絵（クリア／失敗）の直しかた

user から「よくやったの絵の出方が違う。もっとゆっくり出る」と指摘されて
入れたところ。**触るのはここ三つだけ。**

### 1. どこにあるか

| | |
|---|---|
| 演出の定数 | `src/app.h` の `OVER_PASSES` / `OVER_STRIDE` / `OVER_FRAMES` |
| 進める所 | `src/app.c` の `app_tick`、`a->overStep` を増やす節 |
| 描く所 | `src/app.c` の `draw_play` の末尾、`a->result != RESULT_PLAYING` の中 |

`overStep` は面が始まるとき（`app_play`）とクリア／失敗になったとき
（`app_key` の中）に 0 に戻す。

### 2. 原作がやっていること（変えてはいけない部分）

`FUN_23b0_0440`（16 色）:

```
ax = 0
外: bx = ax
  内: out 0xa6,1 で絵のページを読み、4 プレーンを OR して cx
      out 0xa6,0 で表示ページへ戻り、not cx で「絵が無い所」
      各プレーン: [bx] &= cx ; [bx] |= その語
      bx += 0x2a                42 バイト進む
  while bx < 0x8000
  ax += 2
while ax != 0x2a                21 回、開始は 0,2,4 … 40
```

* **21 段**（`OVER_PASSES`）、**42 バイト刻み**（`OVER_STRIDE`）、
  開始は 0,2,…,40。1 語（2 バイト = 16 画素）ずつ置く
* 42 バイトは 1 行の半分強なので、1 段は斜めに散った疎な模様になる。
  21 段で全画素が埋まる（計算でも 100%）
* 「絵が無い所」は 4 プレーンの OR なので **色 0 が透明**。マスクは要らない

### 3. 速さを変えたいとき

**`OVER_FRAMES` だけ**を変える。1 段あたりのフレーム数で、いまは 2
（21 段 × 2 = 42 フレーム ≒ 0.7 秒）。原作は 1 段ずつ一気に回すだけで
待ちを持たず、実機での速さは VRAM の速さそのままなので、**ここは
この移植の判断**。もっとゆっくりなら 3 か 4、速くするなら 1。

### 4. 見え方の注意（バグに見えるが正しい）

雲が真っ黒でなく**灰色の網**に見えるのは正しい。雲は
**色 1 と色 0 の市松（50% 網）**で、タイルパレットでは色 0 も 1 も `000`
なので、絵だけ見ると真っ黒だが、盤の上では色 0 が透明なので半分透ける。

`CLEARM.CG` と `PEKEM.CG` は**マスクではない**。8 色パス
（`FUN_23b0_04bc`）用のモノクロ版の絵。最初これをマスクとして使っていて、
たまたま似た絵になっていた。

### 5. 撮って確かめる

```
R=uuldllldlurrrrrddllulluldrrrdrrullllrrrulll     面 1 の解
for t in 0 8 20 44; do
  ./tmp/soko_shot.exe screen 1 tmp/rev_$t.png --press $R --ticks $t
done
```

失敗側（`PEKE.CG`）は 71 手歩かせる:

```
P=$(python -c "print('lr'*35+'l')")
./tmp/soko_shot.exe screen 1 tmp/fail.png --press "$P" --ticks 44
```

## この日に user から指摘された読み違い（同じ癖に注意）

1. **起動ロゴを両方同時に出した。** `LOGO.CG` に花王と THINKING RABBIT が
   1 枚に並んでいるのでそう描いたが、根拠が無かった。実際は
   `FUN_23b0_03a9` / `03ef` が **x=216 から 208 画素幅の帯**を上へ送る作りで、
   右半分は置き場。`03ef` が 8000 バイトを x=432 → x=216 へ複写してから
   上げる。**2 つが同時に出ることはない**
2. **BGM 4 をプレイ中の曲にした。** `0x121b` の手前が
   `call FUN_1edb_3182 ; or ax,ax ; je`（4 を鳴らす側）で、
   `FUN_1edb_3182` は**ゴールに乗っていない荷物の数**を数える。0 になるのは
   解けたときだけなので **4 はクリア曲**、プレイ中は入るときの 2
3. **テンポを 600Hz と推測した。** 実際は `0x0202` の
   `レジスタ 0x26 = 0xf0` から `3993600/(1152×16) = 216.67Hz`、
   その 1/4 = 54.17 音楽ティック/秒
4. **Ghidra の逆コンパイルを信じすぎた。** `callf 14d7:001d` を生バイナリで
   走査したら呼び出しは 6 か所で、Ghidra は 4 か所しか拾っていなかった。
   **迷ったら生バイナリを正規表現で走査する**

## 2026-09-04 その 3 — エンディング

`src/app.c` の `end_tick` と `src/app.h` の `END_*`。原作の
`FUN_1edb_40bc` を丸ごと写したもので、手順は app.h のコメントに全文ある。

要点だけ:

* **入口**は面選択の輪の中（1edb:053d）。原作の `FUN_1edb_427c` は 30 マスを
  **画面から読んで**数えるが、移植は同じことを records で言う
  （`all_cleared`）。**戻り道は無い** —— 原作のループにキー判定が無いので、
  移植でもエンディング中はキーもクリックも効かない
* 絵は 2 枚だけ持つ。`endBuf` が 0x80000 の待避（END の絵）、`endTop` が
  裏ページ（スタッフの帯）。原作の page(1)/page(0) の往復はこの 2 枚に畳んだ
* 効果は 3 つとも **21 段・42 バイト刻み**の同じ散らし（クリアの絵と同じ）:
  `SCAT_OVER` = 0440（色 0 は透明）、`SCAT_REPLACE` = 01f5 / 0019（黒ごと）、
  `SCAT_ERASE` = 00f9（黒へ）。速さは `OVER_FRAMES` で、クリアの絵と共通
* **スタッフの帯の読み方が変わっている。** `2406:042f` は 80 バイト × 0x1d 行を
  VRAM の 16000（= 200 行目）へ読み、間の seek が**絶対**（0x910）なので
  **後半 0x910 バイトが b000・b800・e000 の 3 回とも読まれる**。だから色は
  0・1・14・15 の 4 色。`load_staff_strip` がそれ。STAFF1 だけは全画面 2
  プレーン（`2406:0213` が 32000 を 2 回読んで、あとはファイルが尽きる）
* 待ちは原作のミリ秒をそのまま `END_MS()` でフレームに直した
  （5000 / 8192 / 4000 / 7000 / 4096）

撮って見るなら:

```
./tmp/soko_shot.exe screen end tmp/end.png --ticks 430   スタッフロール
./tmp/end_check.exe                                      順番の検定
```

`--ticks` の目安: 120 = END1、430 = スタッフロール、1050 = SPECIAL THANKS、
1400 = COPYRIGHT、1850 = 最後の一行だけ残った状態。

## 次にやること（優先順）

1. ~~**エンディング**~~ 済み（上の節）。取り込まなかったのは
   `FUN_1edb_3796`（mouse まわりの小物）だけ。下調べの控えを残す:

   * **入口**は面選択の輪の中（1edb:053d）。`[0x120d] != 3` のとき
     `FUN_1edb_427c() == 0` なら `FUN_1edb_40bc()`
   * `FUN_1edb_427c` は **30 マスを画面から数える**。各マスの
     `(桁*0x60 + 0x22, 行*0x44 + 0x2a)` を `getpixel` して色 12
     （クリア済みの色）なら数え、そうでなければ +0x0c 右も見る。
     返り値は `30 − 数`。**つまり全 30 面クリアで 0** ——移植は records から
     同じことを言えばよい（画素を読む必要は無い）
   * `FUN_1edb_40bc` は **bgm(1) のあと無限ループ**。全文を写すとこう
     （`out/sbp98.asm` の 1edb:40bc〜1edb:427b。`page(n)` = `23b0:000d` =
     `out 0xa6,n` で**表示ページの切替**、`wait(n)` = `18a3:000e`）:

     ```
     bgm(1)
     FUN_1edb_3796()                      クリアの絵の道でも呼ぶ何か
     loop:
       page(1); 絵 "end1" を読む          16 色なら 2406:094f、8 色なら 2406:0ad2
       page(0); 23b0:00f9(); 2406:010e()
       page(1); 23b0:0180()               ページ 0 → 1 の複写
       page(0); 23b0:01f5()
       wait(5000)
       page(1); 0000:1a83(); 絵 "staff1" を読む     2406:0213
       page(0); ディゾルブ                23b0:0440（8 色は 04bc）
       wait(8192)
       23b0:01f5()
       page(1); 絵 "end2" を読む; 23b0:0180()
       page(0); 23b0:01f5()
       page(1); 0000:1a83(); 絵 "staff3" を読む     2406:042f
       page(0); ディゾルブ
       page(1); 0000:1a83(); 絵 "staff4" を読む     2406:042f
       wait(4000)
       page(0); 23b0:01f5(); ディゾルブ
       wait(7000); 23b0:0019(); wait(4096)
       goto loop
     ```

   * **効果は 3 つとも「21 段の散らし」の仲間**だった。外側はどれも
     `ax = 0,2,…,40`（`cmp ax,0x2a`）で 42 バイト刻み、クリアの絵の
     ディゾルブ（`23b0:0440`）とまったく同じ散らし方:

     | | すること |
     |---|---|
     | `23b0:0440` | 裏ページを**色 0 を透明にして**重ねる（いま移植にあるやつ） |
     | `23b0:01f5` | 裏ページを**黒ごと置き換える**（`ffff` のマスクで語単位に上書き） |
     | `23b0:00f9` | 画面を**黒へ消す**（`~ror(0xf0,cl)` を AND、内側で cl を 4 ずつ 4 回） |
     | `23b0:0180` | 散らしなしの単純複写、ページ 0 → 1 |

     つまりエンディングは「裏ページに絵を置いて、3 種類の散らしで
     入れ替えていく」だけ。移植には 21 段の仕掛けがもうあるので、
     裏ページの持ち方さえ決めれば書ける
   * 小物 2 つも読めた。`2406:010e` は **DS:0x300 のエンディング用パレット
     16 本を setpal で当てる**（`[0x90] != 0` のときだけ）。`1000:1a83` は
     ライブラリの `cleardevice` + `moveto(0,0)`、つまり**いま描いている
     ページを消す**——`page(1); 1a83(); 絵を読む` はどれも
     「裏を消してから絵を置く」
   * 残るは `23b0:0019` だけ（B000 を回転マスクで舐める、最後の待ちの前）
   * 移植には**裏ページ**の概念がまだ無い（`Gfx` は 1 枚）ので、
     どう持たせるかも決める必要がある。クリアの絵のディゾルブは
     「絵は Cg のまま、画面に散らして写す」で済ませたので、同じ手で
     いけるかもしれない
   * スタッフロールの中身は PRODUCED by Hiroyuki Imabayashi / Kao
     Corporation、MUSIC by Kenzo Kumei / MICRO CABIN
2. **チャンネルのずれの再確認。** 直したはずだが user にまだ聴いてもらって
   いない（2026-09-04 に「ほぼ完璧」とは言われた）
3. **音の残り**。もう小さい:
   * コード 253 の変調（ソフト LFO 4 種、`[si+0x15]` の下位 2 ビットで
     `0x0d0c` の `0x0b7a` / `0x0b84` / `0x0bae` / `0x0bcf`）。
     **6 曲のどれも 253 を使わない**ので鳴らせる曲が無い。書いても
     確かめようがない
   * ~~フェードアウト（AH=0x06）~~ 済み。docs/sound.md の「フェードアウト」。
     `[0x0f6e]` の bit 7 を見るソフトエンベロープの正体がこれだった
   * 効果音（AH=0x0c 読み込み / 0x0d 再生）は**この game では使わない**。
     ラッパ FUN_2502_0082（AH=0x0c）と FUN_2502_0094（AH=0x0d）を呼ぶ
     far call が実行ファイルに 1 つも無い
     （`python tools/exedis.py calls 2502:0082`）
   * コード 252 は「ジャンプ」ではなく**チップのレジスタへの直接書き込み**
     だった（`0x0821` → `0x042a`）。直したが、6 曲のどれも使っていない
4. **TRACE。** 手順の記録・再生。1 手 3 ビット（水平か・符号・押したか）で
   1 バイト 2 手、3500 バイト/面 × 32 面 = `TRACE.DAT` の 112000 と一致。
   `WINDOWS.CGM` に TRACE 窓（上下の矢印と START）がある
5. **OPTION と EDIT。** `WINDOWS.CGM` に窓がある（EDIT は STAGE / EDIT PLAY /
   SAVE LOAD / ORIGINAL LOAD / CLEAR BGM / EXIT、TOOL は 壁 / 点 / ラビ君 /
   荷物 と EXIT）。`usermen.dat` がユーザ面のファイル
6. ~~**記録の保存**~~ 済み（2026-09-04）。`SBPUSER.DAT` の 310 バイト
   （10 バイト × 31、記録は各レコードの先頭ワード。`FUN_2329_07c6` が
   `fread(&DS:0x3ee4, 10, 0x1f)` で読み、`FUN_1edb_3d80` が 2 バイト書いて
   読み直す）を base64 で localStorage に。`app_user_save` /
   `app_user_load`、頁側は `soko_user_*`。
   **1 つだけ落とした**: 記録のある面をもう一度クリアしたとき、原作は
   「前回:%05d 今回:%05d steps でした、今回の手順を保存しますか？」を出して
   マウスの左右で決める（左=保存）。この文はテキスト画面に本体のフォント
   ROM で出るもので、ROM は同梱できないから描けない。移植は常に保存する。
   `TRACE.DAT` のほうは下の 4 番と一緒
7. モノクロモード。`SBP98.DOC` に「起動時に SHIFT を押しっぱなしで
   モノクロ」と書いてあり、`[0x90]` がその旗で 4 プレーンと 3 プレーンを
   切り替える

## 作業のしかた（守ること）

* **GUI を開かない。** PNG に描いて `Read` で見る
* **WebGL 禁止**（対象機に無い）。ソフトで描く
* **推測で埋めない。** 分からないところは binary を読み直すか、分からないと
  書く。この日は 4 回指摘された（上の節）
* **Ghidra を信じすぎない。** 逆コンパイルは far call を落とすし、タイムアウト
  する関数もある。番地が要るときは `python tools/exedis.py fn 1edb:40bc` で
  生を読む（`out/sbp98.asm` は clone に入っていないし、Ghidra が無いマシンも
  ある）。それでも足りなければ生バイナリを正規表現で走査する
* **「教科書どおり」は根拠ではない。** PC-98 のプレーンとビットの対応は
  教科書どおりに書いてあって、原作は違うことをしていた。原作の中に同じことを
  言っている表が無いか先に探す（2026-09-04 の節）
* Bash のヒアドキュメントは**バックスラッシュを食う**。`\n` を含む C や JS を
  流し込むと `s.replace` が**黙って空振りする**。python のパッチは
  **scratchpad のファイルに書いてから走らせる**
* Windows のファイル名は大文字小文字を区別しない

## 検査（全部通ることを確認してからコミット）

```
sh tools/build.sh check          全部ビルドして検査して PNG を吐く
./tmp/game_check.exe             30 面のデータと規則、60 項目ほど
./tmp/sound_check.exe            音程・音量・ずれ・6 曲
./tmp/sound_check.exe wav 0 8    tmp/bgm0.wav に 8 秒
node tests/wasm_check.js         wasm を node で。画面遷移・曲番号・音の信号
```

node は PATH に無いので
`PATH="/c/prog/emsdk/emsdk/node/22.16.0_64bit/bin:$PATH"` を前に付ける。

## 解析の道具

```
python tools/lzh.py   orig/*.lzh orig       書庫を開く
python tools/fat12.py orig/*.fdi disk       ディスクを開く
python tools/men.py   disk/SBPMEN.DAT       30 面を絵にする
python tools/men.py   disk/SBPMEN.DAT --c   30 面を C の表にする
python tools/cg.py    disk/TITLE.CG t.png --pal 0
python tools/bgm.py   disk/SBPBGM0.BGM 0 --bars    .BGM を MML に起こす
python tools/solve.py disk/SBPMEN.DAT 1     面を解いて手順を出す
python tools/mmdis.py disk/MMD2.SYS map 0x78 0x264 音源ドライバを読む
python tools/exedis.py fn 1edb:40bc         本体を Ghidra 無しで読む
python tools/cgpy.py                        （import 用）絵を python で測る
python tools/strings.py tmp/SBP98.BIN
```

**`out/` は .gitignore なので clone には入っていない。** 作り直しかたは
下にある。Ghidra を入れる前に作った軽い読み方もそのまま使える:

```
python tools/exedis.py fn 1edb:40bc 0x80    そこを逆アセンブル
python tools/exedis.py calls 24d7:001d      そこを呼ぶ far call を全部
python tools/exedis.py data 0x40e           DS:offset を 16 進とテキストで
python tools/exedis.py str 0x41b            DS:offset の文字列
python tools/exedis.py find 9a 1d 00 d7 14  バイト列を探す
```

番地の対応は `tools/exedis.py` の先頭に書いてある。要点は 2 つで、
**ファイル位置 = 0x1200 + ((Ghidra のセグメント − 0x1000) << 4) + オフセット**、
**DS はファイルの 0x1aea0**。実行ファイル自身の far call が持っている
セグメントは Ghidra の表示より 0x1000 小さい。

Ghidra の結果は `out/sbp98.c`（**395 関数のうち 387 本**。8 本は逆コンパイラ
が落ちる ——`out/sbp98.asm` の 18682 命令の逆アセンブルが要るのはそのため）。**2026-09-04 にこの作業機にも
Ghidra 12.1.3 と Temurin 21 を入れた**（`C:\prog\ghidra\`。パスは
`~/.claude/CLAUDE.md` にも控えてある）。作り直すなら

```sh
export JAVA_HOME='C:\prog\ghidra\jdk-21.0.12.1+1'
GH="C:\prog\ghidra\ghidra_12.1.3_PUBLIC\support\analyzeHeadless.bat"

# 初回だけ。取り込みと解析で 3 分ほど
cmd //c "$GH" ghidra_proj sbp -import disk/SBP98.EXE -overwrite

cmd //c "$GH" ghidra_proj sbp -process SBP98.EXE -noanalysis \
    -scriptPath tools/ghidra -postScript DumpAsm.java out/sbp98.asm
cmd //c "$GH" ghidra_proj sbp -process SBP98.EXE -noanalysis \
    -scriptPath tools/ghidra -postScript DumpAll.java out/sbp98.c
```

`ghidra_proj/` も `out/` も .gitignore。`DumpAll` は 10 分ほどかかる
（逆コンパイラが重い関数で粘る）ので、途中で切らないこと。

## 形式は全部 docs にある

* [docs/format.md](docs/format.md) — ディスク、`SBPMEN.DAT` の 30 面、
  `.CG` / `.CGM` の展開、パレット、`FONT.CG`、表示寸法
* [docs/sound.md](docs/sound.md) — MMD2 ドライバ（`INT 0xD2` の API）、
  MML の全文法、音程表、`.VOI` の割り付け、テンポ、曲と場面の対応

## もう一つの移植

**ロードモナーク**が `C:\prog\claude2\lord_monarch_wasm` にあり、
そちらの `RESUME.md` に引継ぎがある。この日そちらも 8.5 倍速だったのを
直した（`[0x32d1]` に 8 を入れて垂直同期で減らす作り）。
